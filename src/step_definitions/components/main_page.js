let global = require('../components/global')

var mainPage = {
    // Create all the selectors. 
    userMenu: '.main-menu__user__wrapper',
    messageInfo: '.messages__message-text',
    global
};


module.exports = mainPage;