let global = require('../components/global')

var resultsPage = {
    // Create all the selectors. 
    searchResultsTitle: "#department-title",
    resultsSelect: '.rs-infinite-scroll',
    resultsChild: './child::div',
    global
};

module.exports = resultsPage;